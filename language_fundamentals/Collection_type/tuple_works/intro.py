
num_tup = (200,300,500,300)

"""
tuple is defined using () , duplicates are allowed, oredered,
"""
print(num_tup) # (200,300,500,300)
print(num_tup[0]) #200

num_tup[0] = 100 # error, tuple is immutable